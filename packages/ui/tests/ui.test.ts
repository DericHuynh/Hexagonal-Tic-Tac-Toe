import { describe, it, expect } from 'vitest';
import * as ui from '../src/index';

describe('ui package', () => {
  it('exports UI components', () => {
    // The ui package should export React components
    // This is a placeholder test to ensure the package structure is valid
    expect(ui).toBeDefined();
  });
});
